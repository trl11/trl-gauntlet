# CLAUDE.md

Guidance for coding agents working in this repository.

## Overview

Gauntlet runs test suites. Suites declare themselves in a `suite.yaml`, write
artifacts to a directory Gauntlet provides, and are otherwise independent of it.

Read [`docs/contract.md`](docs/contract.md) before changing anything that
crosses that boundary.

## Constraints

- No suite-specific code in `packages/gauntlet/`. Behaviour a suite needs is
  declared in its manifest and read generically.
- `gauntlet-suite` depends on pydantic and pyyaml. SSH support is the optional
  `remote` extra. Do not add required dependencies.
- No schema files in the tree. `gauntlet_suite.contract` is the definition;
  JSON Schema is generated by `gauntlet schema <name>` and
  `GET /api/schemas/{name}`.

## Layout

```
packages/gauntlet-suite/   library suite authors install
packages/gauntlet/         application: discovery, supervisor, API, UI
suites/                    built-in and reference suites
packages/gauntlet/src/gauntlet/scaffold/   suite scaffolder and its templates
docs/                      contract specification and guides
```

## Commands

| Task | Command |
|---|---|
| Setup | `make setup` |
| Devcontainer | `make dev` / `make dev-stop` |
| Run with reload | `make run` |
| format-check, lint, typecheck, test | `make check` |
| Tests only | `make test` |
| Contract checks | `make verify-run` |
| Scaffold a suite | `make new-suite NAME=x [TEMPLATE=shell]` |

Run `make check` before committing. Run `make verify-run` as well when
changing the launcher, the contract models, or the conformance checker.

## Behaviour to preserve

- A run without `verdict.json` is recorded as `error`, not `failed`.
- Gauntlet creates the run directory and passes `GAUNTLET_RUN_DIR`.
- Overrides not declared in a manifest are rejected, not forwarded.
- `suite_environment()` prepends Gauntlet's `bin` to `PATH` and the suite
  directory to `PYTHONPATH`. Every code path that spawns a suite process uses
  it.
- Discovery collects manifest errors into `SuiteCatalog.errors` rather than
  raising.

## Contract changes

Changing a model in `gauntlet_suite.contract` is a contract change. Additive
optional fields keep `apiVersion: 1`. Changes that invalidate a conforming
suite increment it.

## Testing

Tests construct real suite directories on disk; see
`packages/gauntlet/tests/conftest.py`.

A new check in `gauntlet.conformance` needs a test with a suite that fails it.

## Style

- `from __future__ import annotations` in every module.
- `Path` over `str` for filesystem arguments.
- Docstrings on public API. Comments explain non-obvious logic and describe
  current behaviour only.
- Alphabetical ordering where order is not otherwise meaningful.
- Fix lint failures rather than adding ignores.
