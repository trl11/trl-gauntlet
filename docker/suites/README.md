# Suites mounted into the server container at /suites, on top of the built-in
# ones the image carries. Empty by default: discovery skips a root with
# nothing in it. Set GAUNTLET_SUITES to mount a directory elsewhere instead.
